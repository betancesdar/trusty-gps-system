# ═══════════════════════════════════════════════════════════════════
# Trusty GPS System — Production Environment Template
# Copy to .env.prod and fill in real values before deploying.
# NEVER commit .env.prod to git.
# ═══════════════════════════════════════════════════════════════════

# ── Database ─────────────────────────────────────────────────────
POSTGRES_USER=gps_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=gps_prod_db

# ── Backend API ──────────────────────────────────────────────────
JWT_SECRET=CHANGE_ME_RANDOM_64_CHAR_SECRET
DEFAULT_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# OpenRouteService API Key — get one at https://openrouteservice.org
ORS_API_KEY=CHANGE_ME_ORS_KEY
ORS_API_URL=https://api.openrouteservice.org

# CORS — comma-separated list of allowed origins (no trailing slash)
ALLOWED_ORIGINS=https://admin.trustygps.app

# ── Frontend ─────────────────────────────────────────────────────
# Baked into the JS bundle at Docker build time
NEXT_PUBLIC_API_URL=https://api.trustygps.app
NEXT_PUBLIC_MAP_CENTER_LAT=40.4168
NEXT_PUBLIC_MAP_CENTER_LNG=-3.7038
NEXT_PUBLIC_MAP_ZOOM=13

# ── Optional Tuning ──────────────────────────────────────────────
# STREAM_TICK_MS=1000          # Location emit interval (ms)
# ENROLL_TTL_SECONDS=600       # QR code validity (10 min default)
# RATE_LIMIT_LOGIN_MAX=10      # Max login attempts per minute per IP
# RATE_LIMIT_ACTIVATE_MAX=30   # Max activate attempts per minute per IP
# ORS_GEOCODING_CACHE_TTL=86400
